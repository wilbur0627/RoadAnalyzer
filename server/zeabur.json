{
  "$schema": "https://schema.zeabur.app/template.json",
  "name": "road-analyzer-server",
  "build": {
    "type": "dockerfile",
    "dockerfile": "Dockerfile"
  },
  "env": {
    "ROAD_ANALYZER_ENV": "production",
    "PORT": "8080"
  }
}
